NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# Server-only. Must match the backend and must never use a NEXT_PUBLIC_ prefix.
OUTBOUND_EMAIL_AUTH_TOKEN=
# mock = synthetic fixtures; api = real requests with no fallback.
NEXT_PUBLIC_DATA_MODE=mock
